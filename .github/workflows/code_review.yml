name: Python Code Review

on:
  pull_request:
    branches: [ main, develop ]

jobs:
  code-review:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install tools
        run: |
          python -m pip install --upgrade pip
          pip install ruff black mypy

      # ✅ Lint + Code Smell Review (Replaces flake8 & pylint)
      - name: 🔍 Ruff Lint
        run: ruff check .

      # ✅ Formatting check
      - name: 🎨 Black Format Check
        run: black . --check

      # ✅ Type safety
      - name: 🧠 MyPy Type Check
        run: mypy .
